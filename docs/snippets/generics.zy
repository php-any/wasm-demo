// 类泛型示例：使用内置泛型类 DB<T>
// 按文档，仅支持“类泛型”，不使用函数泛型

// 定义一个“类泛型” Box<T>，演示仅类泛型的写法

class Box<T> {
  public T $value;
}

// 使用 Box<int>
$intBox = new Box<int>();
$intBox->value = 42;
echo "Box<int>: " . $intBox->value . "\n";

// 使用 Box<string>
$strBox = new Box<string>();
$strBox->value = "hello";
echo "Box<string>: " . $strBox->value . "\n";



